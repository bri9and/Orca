# Security Review

Short alias for a full security review. Runs the same scan as `/secscan`.

## Trigger
`/secrev [project-path]` — defaults to the current working directory.

## Procedure
Run the full `/secscan` procedure from `skills/secscan/SKILL.md`. Read that file and execute all 8 phases against the target directory.
